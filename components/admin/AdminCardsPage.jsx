import AdminCardsFilter from '@/components/admin/AdminCardsFilter'

// ... di dalam return, ganti blok {/* Filters */}:
<AdminCardsFilter
  levels={levels}
  modules={modules}
  levelId={levelId}
  moduleId={moduleId}
/>